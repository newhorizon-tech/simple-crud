setup:
  addons:
    - plan: heroku-postgresql
      as: db
build:
  docker:
    web: Dockerfile
run:
  web: yarn install
  web: bundle exec rails webpacker:install
  web: bunlde exec rails assets:precompile
  web: bundle exec rails s -p 3000 -b '0.0.0.0
