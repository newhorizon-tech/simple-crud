setup:
  addons:
    - plan: heroku-postgresql
      as: db
build:
  docker:
    web: Dockerfile
run:
    web: bundle exec rails s -p 3000 -b '0.0.0.0' \
         bash -c "/usr/src/app/bin/webpack-dev-server"
